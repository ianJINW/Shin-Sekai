{
  "name": "shin-sekai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c cyan,magenta \"npm --workspace=client run dev\" \"npm --workspace=server run dev\"",
    "build": "concurrently -n CLIENT:BUILD,SERVER:BUILD -c yellow,green \"npm --workspace=client run build\" \"npm --workspace=server run build\"",
    "install-all": "npm install",
    "test": "concurrently -n CLIENT:TEST,SERVER:TEST -c blue,red \"npm --workspace=client test\" \"npm --workspace=server test\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
